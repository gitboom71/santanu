---
import { calculatorTranslations } from "@/utils/i18n";

interface Props {
  lang?: "en" | "bn";
}

const { lang = "en" } = Astro.props;
const t = calculatorTranslations[lang] || calculatorTranslations.en;
const contactLink = lang === "bn" ? "/bn/contact" : "/contact";
---

{t.title}
{t.subtitle}

{t.monthlyExpense}

{t.existingCover}

{t.totalLiabilities}

{t.existingSavings}

{t.calculateBtn}

{t.resultHeader}

{t.recommendedCover}
₹0

{t.shortfallNotice}
₹0

{t.adequateNotice}
{t.disclaimer}

{t.ctaConsult}
